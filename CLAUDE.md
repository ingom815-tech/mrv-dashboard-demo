# CLAUDE.md — 디지털 MRV 대시보드 (테스트 버전)

## 프로젝트 목적
- 삼양식품 원주공장 냉열원·공조 시스템 공동진단 제안(제안서 v5)에서 제시한 디지털 MRV 기능이 실제로 동작하는 것을 보여주는 **테스트 화면**을 만듦
- 설계 기준 문서: `docs/대시보드_설계_지시서_v1_0.md` (원본 docx 동봉). 화면 구성·KPI·상태코드·색상·검수기준은 이 문서를 따름
- 서버·DB 없음. **정적 사이트로 GitHub Pages에 배포**함. 합성데이터를 브라우저에서 생성·산정함
- 브랜딩(로고·회사명) 없음. 인포스퀘어·영인 등 회사명은 코드·저장소·메타데이터 어디에도 넣지 않음

## 확정된 결정사항 (변경 금지)
- 기준선 모델: `kWh/일 = a + b × 냉방도일(기준 18℃) + c × 생산량(ton/일)` OLS. 표시용 적합도 기준 R² ≥ 0.75, CV(RMSE) ≤ 25%(ASHRAE Guideline 14)
- 불확도(모델·계측 불확도 범위)는 **화면에 넣지 않음**
- 냉매 비산배출은 전력 감축량과 **합산하지 않고 별도 항목**으로 표기 (보충량 kg × GWP ÷ 1000, 초기 충전은 배출 아님)
- 배출계수는 **등록·적용·버전관리 기능**으로 관리 (값·단위·출처·기준연도·유효기간·상태). 변경 시 새 계산버전 생성
- 데이터 기간은 테스트용으로만 취급 (기준기간 2025년, 보고기간 2026-01~06, 개선 설비 가동 2026-01-01 고정)
- 모든 화면·보고서·다운로드에 `DEMO / 합성데이터`, `공식 MRV 사용 불가` 표기. 레코드 `data_origin = SYNTHETIC`
- 절감량·개선효과는 "데모 가정값"으로만 표기하고 실제 기대성과처럼 쓰지 않음

## 재사용할 산정 엔진 (검증 완료, 그대로 사용)
- `engine/synth.js` — 합성데이터 생성기(15분 주기, 15태그, 물리 일관성, 품질 이벤트 삽입). 결정론적(seed)
- `engine/mrv.js` — 일별 집계, OLS 기준선, 조정 기준선(+승인된 비일상적 조정), 절감량, 온실가스(전력), 냉매 별도, 데이터 품질 통계, 설비 KPI
- `node test/engine.test.mjs` 로 회귀 테스트. 화면 작업 중 엔진을 고치면 이 테스트를 통과시킬 것
- 원천값(`rows[].v`)은 수정하지 않음. 정제·계산 결과는 별도 객체로만 만듦

## 기술 스택 (권장)
- Vite + React + TypeScript, Tailwind CSS
- 차트: Recharts 또는 Apache ECharts (막대·선·영역만, 3D·게이지 금지)
- 상태: 경량(zustand 또는 useReducer) + localStorage 영속(배출계수·승인·감사로그·가정값)
- 배포: `vite.config` base 경로 설정 + GitHub Actions(`actions/deploy-pages`) 또는 `gh-pages` 브랜치
- 외부 CDN 의존 최소화. 웹폰트는 Pretendard 허용

## 화면 (지시서 2.2 메뉴 구조 그대로, 6개)
1. 종합 성과 — 성과 요약 화면. 상단바(사업장·경계·보고기간·갱신시각·MRV 상태) / KPI 6종(조정 기준선·실제·절감·탄소·비용·데이터 신뢰도) / 중심차트: 조정 기준선 vs 실제(절감영역, 보정변수 보조축, 결측·제외 구간 구분) / 신뢰도(수집·정상·결측·추정, 교정·모델 상태) / 설비별 기여 / 이슈 / 하단 추적성(모델·배출계수·계산버전·승인자·증적)
2. 기준선·성과분석 — 상단 고정 정보(대상·기준기간·보고기간·모델버전·산정상태), 조정 전/후 기준선 비교, 사용 변수만 강조, 비일상적 조정 이벤트(사유·기간·승인자), 모델 통계는 전문가용 접힘 영역
3. 설비 성능 — COP·kW/RT·시스템 kW/RT·ΔT·접근온도·펌프동력, 효율저하 구간 표시, 설비군별 KPI 표
4. 데이터 품질 — 수집률·정상률·결측률·추정률(기간·태그별), 상태코드 7종, 계측기 정확도·교정·만료, 원천값/정제값 별도 조회, 이슈와 산정 영향 연결
5. 보고·검증 — 월별 확정성과, 냉매 별도 표, 계산버전·배출계수, 검토→승인 워크플로우(역할 분리), 변경이력·감사로그, 보고서(인쇄용)·JSON·CSV
6. 기준정보 관리 — 설비 계층, 센서·태그(단위·주기·정확도·교정), 배출계수 관리, 냉매 GWP, 가정단가, 사용자·권한, 데모 가정값(재생성)

## 디자인 규칙 (지시서 7장)
- 화이트 배경 + Dark Navy `#122B4D`. MRV Blue `#1E63C6`(기준선·정보), Verified Teal `#159F9E`(감축성과·정상), Review Orange `#C77A1A`(검토·추정), Risk Red `#B33B3B`(오류·제외), Surface Gray `#F4F6F8`
- 기준선·실제값 색상은 전 화면 동일. 색상 외에 선 종류·마커·텍스트 라벨 병기
- 페이지 제목 24~30px 결론형 한 줄, KPI 숫자 28~40px(단위·기간 인접), 본문·표 13~16px 이하로 축소 금지
- 카드 남발 금지. 정보 그룹은 정렬·간격·배경 차이로 구분. 그라데이션·발광·과도한 그림자 금지
- 첫 화면은 스크롤 전에 감축성과·신뢰도·승인상태가 보여야 함

## v1(이전 시도)의 문제 — 반복하지 말 것
- `reference/v1-screens/` 참고. 바닐라 JS 단일 파일로 한 번에 작성, 시각 QA 부족
- 표가 길고 줄바꿈이 많아 밀도 조절 실패(데이터 품질·기준정보 화면)
- 카드형 패널 반복으로 관제화면처럼 보임 — 지시서 1.3 금지사항에 해당
- 상단 필터·KPI·차트가 각각 따로 놀고 위계가 약함

## 완료 기준 (지시서 9장)
- 메인 화면에서 감축량·절감률·탄소감축·데이터 신뢰도·승인상태가 즉시 보임
- 성과값 → 모델·보정변수 → 원천데이터·증적까지 추적 가능
- 배출계수·비일상적 조정 변경 시 기존 확정본 보존 + 새 버전 생성, 승인 완료 결과는 일반 사용자가 수정 불가
- 실측·추정·합성 데이터가 색·라벨로 구분되고, 1366px에서 잘림 없음
- `npm run build` 산출물이 GitHub Pages에서 동작. 엔진 테스트 통과

## 작업 방식
- 화면 하나씩 만들고 브라우저로 확인한 뒤 다음 화면으로 넘어감 (한 번에 6개 통으로 만들지 않음)
- 각 화면 완료 시 1366px·1440px 스크린샷을 남김
- 커밋 메시지·주석·문서는 한국어
